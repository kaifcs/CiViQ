// Supervisor settings — not yet built; the admin and officer settings screens
// are the implemented reference. Renders bare content because AppRouter already
// wraps this route in SupervisorLayout.

import PlaceholderPage from "../../components/PlaceholderPage"

export default function SupervisorSettings() {
  return <PlaceholderPage title="Settings" phase={5} />
}
